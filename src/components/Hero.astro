---
// HeroSection.astro
---

<section class="relative min-h-[500px] flex items-center justify-center bg-gradient-to-br from-gray-50 to-white px-4">
  <div class="max-w-4xl mx-auto text-center">
    
    <!-- Encabezado principal -->
    <div class="">
      <h1 class="text-5xl md:text-6xl lg:text-7xl font-light text-black mb-2 tracking-tight">
        Tarjetas de lealtad
      </h1>
      
      <div class="relative">
        <h2 class="text-2xl md:text-4xl lg:text-5xl   text-gray-900 mb-4 oi-regular">
          para gente bonita
          <span class="text-red-500 ml-2 inline-block font-bold oi-regular">*</span>
        </h2>
      </div>
    </div>

    <!-- Subtítulo -->
    <p class="text-sm md:text-base text-gray-600 mb-12 max-w-2xl mx-auto leading-relaxed font-light">
      <span class="text-red-500">*</span> Entiéndase por "gente bonita" a clientes que aman tu negocio y quieren más de tu producto.
    </p>

    <!-- Botón CTA -->
    <div class="mt-12">
      <button class="bg-teal-400 hover:bg-teal-500 text-gray-800 font-medium px-8 py-4 rounded-full text-lg ">
        Obtén Loyal Chimp ahora
      </button>
    </div>

    <!-- Elementos decorativos -->
    <div class="absolute inset-0 pointer-events-none overflow-hidden">
      <div class="absolute top-1/4 left-1/4 w-64 h-64 bg-teal-100 rounded-full opacity-20 blur-3xl"></div>
      <div class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-red-100 rounded-full opacity-20 blur-3xl"></div>
    </div>
    
  </div>
</section>

<style>
  .oi-regular {
  font-family: "Oi", serif;
  font-weight: 400;
  font-style: normal;
}


  @media (max-width: 768px) {
    .decorative-text {
      font-size: 2.5rem;
    }
  }

  button {
    background: linear-gradient(135deg, #7dd3fc 0%, #67e8f9 100%);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
  }

  button:hover {
    background: linear-gradient(135deg, #67e8f9 0%, #06b6d4 100%);
    transform: translateY(-2px);
  }
</style>
